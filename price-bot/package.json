{
  "name": "pocket-option-price-capture",
  "version": "1.0.0",
  "description": "Pocket Option OTC live capture + Phase 1 feature/label collector for an AI/ML prediction pipeline",
  "type": "module",
  "scripts": {
    "start": "tsx capture.ts",
    "render:start": "tsx capture.ts",
    "capture": "tsx capture.ts",
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "postinstall": "playwright install chromium --with-deps"
  },
  "dependencies": {
    "playwright": "^1.61.1",
    "ws": "^8.21.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@types/ws": "^8.18.1",
    "tsx": "^4.21.0",
    "typescript": "~5.8.2"
  }
}
